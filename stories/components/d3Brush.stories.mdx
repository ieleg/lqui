import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import MyBar from '../../src/components/D3/brush.vue';

<Meta title="D3/brush" component={MyBar} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { MyBar },
  template: `<my-bar v-bind="$props" />`
});

# bar-chart
brush 事件触发失败，d3.event未保存相关参数(这个是版本问题，7的事件参数都以回调传递而非存在一个全局的变量里)
## bar-chart(不显示刷新下)
<Canvas>
  <Story
    name="默认"
    args={{
      n: 1,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="默认" />
