import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import WordCloud from '../../src/components/D3/word-cloud.vue';

<Meta title="D3/word-cloud" component={WordCloud} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { WordCloud },
  template: `<word-cloud v-bind="$props" />`
});

# cloud-chart
## cloud-chart(不显示刷新下)
<Canvas>
  <Story
    name="默认"
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="默认" />
