import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import MyLine from '../../src/components/D3/line2.vue';

<Meta title="D3/line" component={MyLine} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { MyLine },
  template: `<div style="height: 500px;width:500px"><my-line v-bind="$props" /></div>`
});

# linWe-chart
傅里叶级数fft,观察可得到级数展开越多越趋向方波，且毛刺越少
## line-chart
<Canvas>
  <Story
    name="默认"
    args={{
      n: 1,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="默认" />
