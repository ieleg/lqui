import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import MyArea from '../../src/components/D3/area.vue';

<Meta title="D3/area" component={MyArea} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { MyArea },
  template: `<my-area v-bind="$props" />`
});

# area-chart
当使用 band和 point比例尺时ticks没有作用，但是 axis.tickValues. 和 axis.tickFormat 不受比例尺类型限制
  
当x轴为日期的时候，如何让ticks自适应的变化呢？即放大的时候ticks增加，缩放时候ticks减少。
ticks分段只可分2 5 10及其最小公倍数作为间隔，但是在设置时间ticks的时候5没有生效。

目前是手动调缩放比例然后去设置对应的ticks
## area-chart(不显示刷新下)
<Canvas>
  <Story
    name="默认"
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="默认" />
