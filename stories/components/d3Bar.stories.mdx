import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import MyBar from '../../src/components/D3/bar.vue';

<Meta title="D3/bar" component={MyBar} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { MyBar },
  template: `<div style="height: 500px;width:500px"><my-bar v-bind="$props" /></div>`
});

# linWe-chart
傅里叶级数fft,观察可得到级数展开越多越趋向方波，且毛刺越少
## line-chart
<Canvas>
  <Story
    name="默认"
    args={{
      n: 1,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="默认" />
