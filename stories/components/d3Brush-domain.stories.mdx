import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import MyBar from '../../src/components/D3/brush-domain.vue';


<Meta title="D3/brush(domain)" component={MyBar} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { MyBar },
  template: `<my-bar v-bind="$props" />`
});

# bar-chart

这个版本通过改变x轴的定义域domain来改变其视图范围，之前那个拉宽缩小值域的方法有点反直觉

每次拖动会更新x轴的定义域，视图上rect图元会动态增删

## bar-chart(不显示刷新下)
<Canvas>
  <Story
    name="默认"
    args={{
      n: 1,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="默认" />
